# Cadder

Cadder is a Windows tray daemon scaffold for routing project-local `caddy.exe` invocations into one persistent real Caddy runtime.

## Build

```powershell
.\build.ps1
```

See [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) for the initial process boundaries.
